# Rules naming the files `static-cmd-dispatcher` generates.

# One source per deployment topology, e.g. `Top` -> `TopStaticCmdDispatchAc.cpp`.
# A module without a deployment topology matches nothing and generates nothing.
[[group]]
node = "DefTopology"
where = '$.is_deployment'
generate = ["StaticCmdDispatchAc.cpp"]
